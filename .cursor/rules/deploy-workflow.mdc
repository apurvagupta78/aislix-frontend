---
description: Agent must push and publish deploys — never ask the user to do it
alwaysApply: true
---

# Deploy workflow

After frontend or backend fixes that should go live:

1. **Always commit, push, and publish yourself** — do not ask the user to push, publish, or redeploy.
2. **Frontend (`aislix-frontend`)**: commit → `git push origin main` → Lovable auto-deploys to aislix.com. If auto-deploy is slow or blocked, open the Lovable project and publish from there (user session may be required in browser).
3. **Backend (`aislix-backend`)**: commit → push the branch Railway watches → confirm redeploy when relevant.
4. Tell the user what was deployed and when to retest — not what they need to click.

Never end with "you should push/publish" or "please publish in Lovable" as the required action for the user.
